# Google Calendar API Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com/

# Your Google Calendar API Key (client-side accessible)
NEXT_PUBLIC_GOOGLE_CALENDAR_API_KEY=your_api_key_here

# The calendar ID you want to display (client-side accessible)
# For ASU CBC, this might be something like: asu.edu_primary or a specific calendar ID
NEXT_PUBLIC_GOOGLE_CALENDAR_ID=asu.edu_primary

# Optional: Timezone for the calendar (defaults to America/Phoenix)
NEXT_PUBLIC_CALENDAR_TIMEZONE=America/Phoenix

# Email Configuration for Job Applications
# Set up SMTP for sending application emails
# You can use services like Gmail, Outlook, or email providers like SendGrid

# SMTP Configuration (server-side only)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# Email address to receive job applications
RECIPIENT_EMAIL=careers@asucbc.com
